<%= form_with(model: admin) do |form| %>
    <% if admin.errors.any? %>
        <div style="color: red">
        <h2><%= pluralize(admin.errors.count, "error") %> prohibited this admin from being saved:</h2>

        <ul>
            <% admin.errors.each do |error| %>
            <li><%= error.full_message %></li>
            <% end %>
        </ul>
        </div>
    <% end %>
    <div class="input-wrapper">
        <div>
        <%= form.text_field :name, placeholder: "Name", class: "input-box", :required => true %>
        </div>
        <div>
        <%= form.text_field :email, placeholder: "Email", class: "input-box", :required => true %>
        </div>
        <div>
        <%= form.password_field :password, placeholder: "Password", class: "input-box", :required => true %>
        </div>
        <div>
        <%= form.submit 'Create', :class => 'button button1' %>
        </div>
    </div>
<% end %>